{
  "name": "ode-css",
  "version": "1.0.0",
  "description": "The style of Hugo ode theme",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "stylus src/style.styl -o ./dist/css/style.css",
    "dev": "stylus -w src/style.styl -o ../blog/static/css/style.css"
  },
  "author": "ode",
  "license": "MIT",
  "devDependencies": {
    "stylus": "^0.54.5"
  }
}
